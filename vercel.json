{
  "buildCommand": "pnpm build",
  "installCommand": "pnpm migrate:deploy && pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/trpc/:path*", "destination": "/api/trpc/:path*" }
  ]
}
