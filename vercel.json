{
  "build": {
    "command": "npm run vercel-build",
    "output": "dist"
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/trpc/(.*)", "dest": "/trpc" },
    { "src": "/api/debug/(.*)", "dest": "/api/debug/client-logs" },
    { "src": "/(.*)", "dest": "/client" }
  ]
}
