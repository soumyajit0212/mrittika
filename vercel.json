{
  "build": {
    "env": {
      "NODE_VERSION": "20"
    }
  },
  "installCommand": "corepack enable && corepack prepare pnpm@9.12.3 --activate && node -v && pnpm -v && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm build",
  "framework": "vite"
}
